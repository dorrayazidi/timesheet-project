apiVersion: v1
kind: Service
metadata:
  name: timesheet-serv
  namespace: chap4
spec:
  selector:
    app: timesheet
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: NodePort

